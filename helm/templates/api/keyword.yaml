apiVersion: apps/v1
kind: Deployment
metadata:
  name: keyword-api
  labels:
    app: keyword-api
spec:
  selector:
    matchLabels:
      app: keyword-api
  replicas: 1
  template:
    metadata:
      labels:
        app: keyword-api
    spec:
      containers:
        - name: keyword-api
          image: lblod/poc-ai-keywords:latest
          ports:
            - containerPort: 8080
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /
              port: 8080
              scheme: HTTP
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 1
          volumeMounts:
            - name: models
              mountPath: /models
              readOnly: true
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: poc-ai-deployment-model-store
---
apiVersion: v1
kind: Service
metadata:
  name: keyword-api
spec:
  type: ClusterIP
  selector:
    app: keyword-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
